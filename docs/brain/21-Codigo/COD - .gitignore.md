---
id: COD-.gitignore
aliases: [COD-.gitignore]
tipo: codigo
titulo: ".gitignore"
arquivo: ".gitignore"
gerado: true
tags: [codigo]
---

# COD-.gitignore — .gitignore

Implementa [`.gitignore`](../../../.gitignore) — **gerado** por `npm run brain:sync`.

## Onde aparece

```dataview
LIST id, tipo FROM "" WHERE contains(implementacao, this.arquivo) OR contains(testes, this.arquivo)
```

## Conexões

- 🗺️ [[MOC - Codigo]]
