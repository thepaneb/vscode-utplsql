---
id: COD-discovery.ts
aliases: [COD-discovery.ts]
tipo: codigo
titulo: "discovery.ts"
arquivo: "src/discovery.ts"
gerado: true
tags: [codigo]
---

# COD-discovery.ts — discovery.ts

Implementa [`src/discovery.ts`](../../../src/discovery.ts) — **gerado** por `npm run brain:sync`.

## Testes que cobrem

- [[TST - discovery.test.ts]]

## Onde aparece

```dataview
TABLE id, tipo WHERE contains(implementacao, this.arquivo) OR contains(testes, this.arquivo)
```

## Conexões

- 🗺️ [[MOC - Codigo]]
